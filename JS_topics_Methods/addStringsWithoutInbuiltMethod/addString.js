let str = 'Hello'; // Initial string
let newValue = ' World'; // Value to be added

// Adding the new value manually
for (let i = 0; i < newValue.length; i++) {
  str += newValue[i];
}

console.log(str); // Output: "Hello World"
